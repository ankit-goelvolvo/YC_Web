@model IEnumerable<YCWeb.Data.OfficeFacility>

@{
    Layout = null;
}

<p>
    <button onclick="CreateOfficeFacility();" class="btn btn-success">Create</button>
</p>
<div class="tbl-header">
    <table class="table">
        <thead>
            <tr>
                <th>
                    Office Facility
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
                <th></th>
            </tr>
        </thead>
    </table>
</div>
<div class="tbl-content">
    <table>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.OfficeFacilityName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        <a href="#" onclick="DetailOfficeFacility(@item.OfficeFacilityID)">Details</a> |
                        <a href="#" onclick="EditOfficeFacility(@item.OfficeFacilityID)">Edit</a> |
                        <a href="#" onclick="DeleteOfficeFacility(@item.OfficeFacilityID)">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>


<script type="text/javascript">
    $(window).on("load resize ", function () {
        var scrollWidth = $('.tbl-content').width() - $('.tbl-content table').width();
        $('.tbl-header').css({ 'padding-right': scrollWidth });
    }).resize();
</script>